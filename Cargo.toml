[workspace]
members = [
  "txkit-builtin",
  "txkit-cli",
  "txkit-core",
  "txkit-impl",
  "txkit-tests"
]

[patch.crates-io]
txkit-core = { path = "txkit-core" }
txkit-impl = { path = "txkit-impl" }
txkit-builtin = { path = "txkit-builtin" }
